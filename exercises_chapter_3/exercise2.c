// includes header file to the current program
#include <stdio.h>

// function which runs in program execution
// void is argument in function
int main(void){

  // prints all the string to standard output
  // \ n is next line and ; is to end function printf
  printf("In C lower case letters are significant\n");
  printf("Main is function where program execustion begins\n");
  printf("All program statements must be terminated by a semicolon\n");
  // exits the programs by returning 0
  return 0;
}
